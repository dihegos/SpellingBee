SECRET_KEY=change-this-in-local
GUEST_CODE=INVITADO2026

# Optional (Stripe). Leave blank for local tests (use guest).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# Optional DB override. If not set, app uses local SQLite file.
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
